<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.notework.nw.dao.PresetDao">
	
	<select id="getList" parameterType="String">
		select id, name, linkAddress from PresetView where memberId=#{memberId}
	</select>
	<insert id="insert" parameterType="com.notework.nw.entity.Preset" useGeneratedKeys="true" keyColumn="id">
		insert into Preset(name, linkAddress, memberId) values(#{name}, #{linkAddress}, #{memberId})
	</insert>

</mapper>