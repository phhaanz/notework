<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.notework.nw.dao.NoteDao">
		
	<select id="getList" resultType="com.notework.nw.entity.Note">
		select * from Note order by regDate DESC
		limit ${(_parameter-1)*15}, 15 
	</select>
		
	<select id="get" resultType="com.notework.nw.entity.Note">
		select * from Note where id=#{id}
	</select>
	
	<insert id="insert" parameterType="com.notework.nw.entity.Note">
		insert into Note(title, content, writerId, publish)
		values(#{title}, #{content}, #{writerId}, #{publish})
	</insert>
	
	<update id="updateHit" parameterType="com.notework.nw.entity.Note">
		update Note set hit=hit+1 where id=#{id}
	</update>
	
</mapper>